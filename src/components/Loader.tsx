export const Loader:React.FC = () => {
    return (
        <div className={`h-[80dvh] flex items-center justify-center px-24`}>
             <h1 className="font-bold text-center animate-pulse text-4xl">LOADING</h1>
        </div>

    );
}
